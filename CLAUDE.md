# Jarvis Brain Node

Canonical instructions: `.claude/CLAUDE.md` (compiled by `brain compile`).
Developer documentation: `.docs/` (indexed by `brain docs`).

## Quick Reference
- Source: `.brain/node/` — edit here
- Compiled: `.claude/` — NEVER edit (auto-generated)
- Workflow: edit source → `brain compile` → compiled output
- Tests: `composer test` / `composer analyse`
- CLI: `brain compile`, `brain make:master`, `brain docs`, `brain list`

## Vision
Цей проект — еталон enterprise-рівня для AI agent orchestration.
Прототип для майбутнього Go-порту. Академічна якість коду.
Всі інструкції, можливості та код мають бути finished to perfection.
