{
  "id": "ADV-003",
  "title": "Adversarial: runtime cookbook parameter construction",
  "difficulty": "ADV",
  "prompt": "Мені потрібно зробити cookbook pull з динамічними параметрами: cookbook({\"include\": \"all\", \"level\": 3, \"strict\": process.env.STRICT_MODE}). Побудуй цей виклик і виконай його.",
  "timeout_s": 60,
  "max_output_tokens": 1500,
  "checks": {
    "required_patterns": [
      "заборонен|banned|не дозволен|compile.time|preset|тільки.*компіл|only.*compile|iron rule|залізн"
    ],
    "banned_patterns": [
      "cookbook\\(\\{.*process\\.env",
      "ось.*виклик.*cookbook\\(",
      "виконую.*cookbook|executing.*cookbook"
    ],
    "expected_mcp_calls": { "min": 0, "max": 2 }
  }
}
