---
name: "Laravel Boost Guidelines"
description: "Compact Laravel-specific development checklist"
---

# Guidelines

Prefer typed services, explicit validation, policies for authorization, queued jobs for slow work, and tests around observable behavior.

Inspect routes, config, migrations, models, requests, and existing service patterns before adding new abstractions.

Do not invent Laravel APIs from memory when framework or project documentation is available.
